# คู่มือติดตั้ง: แบบประเมิน Future Skills (Digital) → บันทึกลง Google Sheet

ไฟล์ที่เกี่ยวข้อง:
- **future-skills-digital-form.html** — หน้าเว็บฟอร์มธีม Digital ที่ผู้ทำแบบประเมินกรอก
- **FormBackend.gs** — สคริปต์ฝั่งเซิร์ฟเวอร์ รับข้อมูลแล้วบันทึกลง Google Sheet

---

## ขั้นตอนที่ 1 — สร้าง Google Sheet

1. สร้าง Google Sheet เปล่าใหม่ 1 ไฟล์ ตั้งชื่อ เช่น "Future Skills Digital - ผลการประเมิน"
2. ไม่ต้องสร้างชีต/หัวตารางเอง สคริปต์จะสร้างชีตชื่อ **"ผลการประเมิน"** พร้อมหัวตารางให้อัตโนมัติในการบันทึกครั้งแรก

## ขั้นตอนที่ 2 — เปิด Apps Script และวางโค้ด

1. ใน Google Sheet ที่สร้าง ไปที่เมนู **ส่วนขยาย (Extensions) > Apps Script**
2. ลบโค้ดเริ่มต้นในไฟล์ `Code.gs` (หรือไฟล์เริ่มต้น) ทิ้ง แล้ววางเนื้อหาทั้งหมดจากไฟล์ `FormBackend.gs` ที่แนบมาแทน
3. กด **บันทึก** (ไอคอนแผ่นดิสก์ หรือ Ctrl/Cmd+S)

## ขั้นตอนที่ 3 — Deploy เป็น Web App

1. คลิก **Deploy > New deployment**
2. คลิกไอคอนเฟือง เลือกประเภท **Web app**
3. ตั้งค่า:
   - Description: ใส่ชื่ออะไรก็ได้ เช่น "Future Skills Form Backend"
   - Execute as: **Me**
   - Who has access: **Anyone** (จำเป็น เพื่อให้ผู้ทำแบบประเมินส่งข้อมูลได้โดยไม่ต้องล็อกอิน)
4. กด **Deploy**
5. ระบบจะขอ **Authorize access** — อนุญาตสิทธิ์ตามที่ขอ (อาจต้องกด "Advanced" > "Go to [ชื่อโปรเจกต์] (unsafe)" เพราะเป็นสคริปต์ที่คุณเขียนเอง ไม่ใช่แอปที่ผ่านการตรวจสอบจาก Google)
6. คัดลอก **Web app URL** ที่ได้ (รูปแบบ `https://script.google.com/macros/s/xxxxx/exec`)

## ขั้นตอนที่ 4 — เชื่อมฟอร์มกับ Web App

1. เปิดไฟล์ `future-skills-digital-form.html`
2. หาบรรทัดนี้ในส่วน `<script>`:
   ```js
   var WEB_APP_URL = 'PASTE_YOUR_GOOGLE_APPS_SCRIPT_WEB_APP_URL_HERE';
   ```
3. แทนที่ด้วยลิงก์ที่คัดลอกมาจากขั้นตอนที่ 3 เช่น:
   ```js
   var WEB_APP_URL = 'https://script.google.com/macros/s/xxxxx/exec';
   ```
4. บันทึกไฟล์

## ขั้นตอนที่ 5 — ทดสอบ

**ทดสอบแบบเร็ว (ไม่ต้องผ่านฟอร์ม):**
ในตัวแก้ไข Apps Script เลือกฟังก์ชัน `testDoPost` ที่แถบด้านบน แล้วกด **Run** — จะมีแถวข้อมูลตัวอย่างถูกเพิ่มลงชีต "ผลการประเมิน" ทันที

**ทดสอบผ่านฟอร์มจริง:**
เปิดไฟล์ `future-skills-digital-form.html` ในเบราว์เซอร์ (หรืออัปโหลดขึ้นเว็บโฮสติ้ง/แชร์เป็นลิงก์) กรอกข้อมูลและตอบครบทุกข้อ แล้วกด **ส่งแบบประเมิน** — ควรเห็นข้อความ "ส่งแบบประเมินสำเร็จ" และมีแถวใหม่ในชีต

## ขั้นตอนที่ 6 — นำฟอร์มไปใช้งานจริง

ไฟล์ `future-skills-digital-form.html` เป็นไฟล์ HTML แบบสแตนด์อโลน เปิดวิธีใดวิธีหนึ่งได้เลย:
- อัปโหลดขึ้นเว็บโฮสติ้งใดก็ได้ (เช่น GitHub Pages, Netlify, Google Sites แบบฝัง HTML)
- แนบไฟล์แล้วส่งให้เปิดโดยตรงในเบราว์เซอร์ (ใช้งานได้ แต่ไม่เหมาะกับการแชร์ลิงก์สาธารณะ)

---

## หมายเหตุ

- ทุกครั้งที่แก้โค้ดใน `FormBackend.gs` ต้อง **Deploy > Manage deployments > แก้ไข (ไอคอนดินสอ) > Deploy ใหม่** จึงจะมีผลกับ Web app URL เดิม (URL จะไม่เปลี่ยนถ้าใช้ "Manage deployments" แก้ของเดิม)
- ข้อมูลคะแนนแต่ละข้อ (40 ข้อ) และคะแนนรวมแต่ละด้าน (5 ด้าน) จะถูกคำนวณและบันทึกลงคอลัมน์ท้ายตารางให้อัตโนมัติ ไม่ต้องคำนวณเอง
- หากต้องการจำกัดผู้เข้าถึงเฉพาะคนในองค์กร (ไม่ใช่ Anyone) และใช้ Google Workspace for Education/Business สามารถเปลี่ยน "Who has access" เป็น "Anyone within [organization]" ได้ในขั้นตอนที่ 3

## ส่งภาพผลลัพธ์ (สเตตัสการ์ด) ไปที่อีเมลผู้กรอกอัตโนมัติ

ตอนนี้เมื่อกดส่งแบบประเมินสำเร็จ ระบบจะ:
1. จับภาพสเตตัสการ์ด (แถบคะแนน + เรดาร์ + คำแนะนำ) เป็นรูปภาพในเบราว์เซอร์ผู้ใช้ (ใช้ไลบรารี `html2canvas` ที่โหลดจาก CDN — ต้องมีอินเทอร์เน็ตตอนเปิดฟอร์ม)
2. ส่งรูปนั้นแนบไปพร้อมข้อมูลฟอร์มไปยัง `FormBackend.gs`
3. `FormBackend.gs` จะส่งอีเมลกลับไปหาผู้กรอก (ที่อยู่อีเมลจากฟิลด์ "อีเมล" ในฟอร์ม) พร้อมรูปสเตตัสการ์ดฝังอยู่ในเนื้อหาอีเมล เพื่อให้ดูผลย้อนหลังได้ทุกเมื่อ

**ทดสอบเฉพาะส่วนอีเมล:** เลือกฟังก์ชัน `testDoPost` แล้วกด Run เหมือนเดิม — ฟังก์ชันนี้แนบรูปตัวอย่างเล็กๆ ไปด้วย จะมีอีเมลส่งไปที่ `test@example.com` (แก้ที่อยู่อีเมลในฟังก์ชันเป็นของจริงถ้าอยากเห็นอีเมลจริง)

**ข้อจำกัดที่ควรรู้:**
- ถ้าอีเมลผู้กรอกว่างเปล่า ระบบจะยังบันทึกคะแนนลง Sheet ตามปกติ แค่ไม่ส่งอีเมล
- ถ้าโหลด `html2canvas` ไม่สำเร็จ (เช่น ไม่มีอินเทอร์เน็ต หรือ CDN ถูกบล็อก) ระบบจะยังบันทึกข้อมูลได้ตามปกติ แค่ไม่มีรูปแนบในอีเมล
- `MailApp.sendEmail` มีโควตาประมาณ 100 ฉบับ/วันสำหรับบัญชี Gmail ทั่วไป (บัญชี Google Workspace จะได้โควตาสูงกว่า)
- ต้องการต่อยอดเป็นระบบส่งอีเมลผลลัพธ์อัตโนมัติแบบที่เคยทำไว้ก่อนหน้า (Code.gs + StatusCard.html ที่ใช้ลิงก์แทนรูปแนบ) สามารถนำ `onFormSubmit`-style logic มาเพิ่มต่อจาก `doPost` ในไฟล์นี้ได้ — แจ้งได้หากต้องการให้ช่วยรวมสองระบบเข้าด้วยกัน
